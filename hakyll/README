1. stack build --install-ghc
2. stack exec -- site build
3. stack exec -- site deploy
4. Do add and commit and push with git command

If you want to check your post offline, run `stack exec -- site watch'
